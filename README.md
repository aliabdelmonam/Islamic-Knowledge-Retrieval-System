# W&B Logging
### entity (team's name)
which is RAG-Hadith (it's fixed and can't be changed unless we want to change the name of the team)
### project name (project, it's fixed and can't be changed unless we want to change the name of the project)
the project that we will have and insert our run in.
### Run Name (name)
Use a naming convention
### Config
should have the information that u did in ur expermiment, as we can't see the code, log everything u used (generation model, embedding model, indexing technique,
u have dealt with the data differently, etc...)
### log
which are basically the metrics we want to keep track of (so far), and they **must be done on the evaluation test we have defined**.
### finish
Please do this to let W&B know you've finished the experiment.
